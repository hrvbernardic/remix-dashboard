version: '3'
services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2019-latest
    ports:
      - '1433:1433'
    environment:
      # Username is 'SA'
      SA_PASSWORD: 'Mssql123'
      ACCEPT_EULA: 'Y'
